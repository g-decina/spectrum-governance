/// spectrum-common: Shared types and utilities for spectrum-governance
///
/// This crate provides common types, traits, and utilities used across
/// all spectrum crates (red, blue, lens, infra).

pub mod types;

pub use types::*;
